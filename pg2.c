#include <stdio.h>
int main(){
int num;

    // Input for test case 1: 4
    num = 4;
    if (num % 2 == 0) {
        printf("Input: %d Output: Even\n", num);
    } else {
        printf("Input: %d Output: Odd\n", num);
    }

    // Input for test case 2: 7
    num = 7;
    if (num % 2 == 0) {
        printf("Input: %d Output: Even\n", num);
    } else {
        printf("Input: %d Output: Odd\n", num);
    }
    return 0;
}